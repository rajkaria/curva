{
  "name": "@curva/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End-to-end demo: the whole Curva pipeline (derive → terrace → bet → kill-host → lock → ASR attest → resolve → net → settle → receipts) wired from the real packages. Runs headless — the scripted-swarm demo backup and a CI proof the stack composes.",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@curva/terrace-base": "0.1.0",
    "@curva/market-kernel": "0.1.0",
    "@curva/sim": "0.1.0",
    "@curva/crowd-oracle": "0.1.0",
    "@curva/wdk-vault": "0.1.0",
    "@curva/market-catalogue": "0.1.0",
    "@curva/qvac-surfaces": "0.1.0"
  }
}
