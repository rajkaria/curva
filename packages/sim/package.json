{
  "name": "@tifo/sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "In-process multi-peer swarm simulator + adversarial fuzzer. Lamport-ordered, partition-aware model of Autobase linearization; proves byte-for-byte view convergence and every money invariant under churn, partitions, and attacks.",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@tifo/terrace-base": "0.1.0",
    "@tifo/market-kernel": "0.1.0"
  }
}
